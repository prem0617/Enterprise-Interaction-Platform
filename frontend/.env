FRONTEND_URL=http://localhost:5173
# VITE_API_URL is intentionally unset so the app dynamically resolves
# the backend URL from the browser's current hostname (port 3000 by default).
# Uncomment and set below to override:
# VITE_API_URL=http://localhost:3000/api